# Copy this file to `.env` and fill in your YouTube Data API v3 key.
# The build script loads `.env` automatically via `node --env-file-if-exists=.env`.
# NEVER commit your real .env — it is gitignored.
YOUTUBE_API_KEY=
